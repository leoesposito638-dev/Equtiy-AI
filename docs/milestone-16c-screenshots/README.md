# Milestone 16C — Prototype 1.2 alignment screenshots

"Before" is `docs/milestone-16b-screenshots/` (same live backend, same
NVDA/LLY/Discover data, captured just before this milestone). These are
"after," captured the same way (Playwright + the pre-installed Chromium)
against the frontend with all 5 items applied.

| File | Shows |
|---|---|
| `after-01-dashboard.png` | Dashboard — grouped sidebar (RESEARCH / PORTFOLIO / MONITORING, no Settings). Compare to `../milestone-16b-screenshots/after-02-dashboard-lly-row-live.png`. |
| `after-02-nvda-company-page.png` | NVDA company page — identity block (initials avatar + real FMP-sourced one-sentence description), Opportunity Score card gone, ⓘ tooltip icons on every glossary-matched metric label across Score Dimensions/Growth Metrics/Key Financials/Valuation. Compare to `../milestone-16b-screenshots/after-01-company-nvda-full-live.png`. |
| `after-03-nvda-tooltip-open.png` | The Growth category's ⓘ clicked open, showing the "Revenue Growth" glossary popover (WHAT IS IT? / WHY IT MATTERS). |
| `after-04-search-nvidia.png` | Dashboard search box, query "nvidia" (lowercase) — now calling the real, injection-fixed `GET /search` (debounced), not a client-side filter. |

Traceability for the 16 FMP-sourced company descriptions (source URL,
retrieval timestamp, untruncated original text alongside what was actually
stored) is at `docs/milestone-16c-description-sources.json`, generated by
`backend/src/localDev/milestone16cCompanyDescriptionsIngestion.ts`.

See the full Milestone 16C report (published separately) for the search
filter-injection fix and its test coverage, the glossary definitions
flagged as reading absolute, and the rest of the implementation detail.
